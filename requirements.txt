# Compatibility install path for users who do not install the project itself.
# The canonical dependency declarations live in pyproject.toml.
litellm[proxy]==1.92.0
python-dotenv==1.2.2
PyYAML>=6.0.2,<7
